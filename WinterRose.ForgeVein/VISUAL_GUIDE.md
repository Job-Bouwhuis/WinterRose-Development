# WinterRose.ForgeVein - Visual Architecture Guide

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                         │
│                                                               │
│    ┌──────────────────┐  ┌──────────────────┐                │
│    │ NetworkServerBase│  │ NetworkClientBase│                │
│    └──────────────────┘  └──────────────────┘                │
│              ↑                       ↑                         │
└──────────────┼───────────────────────┼─────────────────────────┘
               │                       │
┌──────────────┼───────────────────────┼─────────────────────────┐
│              │    Routing Layer      │                          │
│    ┌─────────────────────────────────┴─────┐                  │
│    │  IRoutingStrategy                      │                  │
│    │  IRelayService                         │                  │
│    │  IStreamRouter                         │                  │
│    │  IClusterDelegationService             │                  │
│    └──────────────────────────────────────┘                  │
└──────────────┬───────────────────────┬─────────────────────────┘
               │                       │
┌──────────────┼───────────────────────┼─────────────────────────┐
│              │ Validation Layer      │                          │
│    ┌─────────────────────────────────┴─────┐                  │
│    │  IValidator<T>                         │                  │
│    │  IValidationPipeline                   │                  │
│    │  IValidationDefinition<T> (Fluent API)│                  │
│    │  IValidationRegistry                   │                  │
│    └──────────────────────────────────────┘                  │
└──────────────┬───────────────────────┬─────────────────────────┘
               │                       │
┌──────────────┼──────────────────────┼──────────────────────────┐
│              │ Packet/Serialization  │                          │
│    ┌─────────────────────────────────┴─────┐                  │
│    │  IPacketRegistry                       │                  │
│    │  IPacketDescriptor                     │                  │
│    │  IPacketSerializer                     │                  │
│    │  IPacketDispatcher                     │                  │
│    │  IPacketHandler<T>                     │                  │
│    └──────────────────────────────────────┘                  │
└──────────────┬───────────────────────┬─────────────────────────┘
               │                       │
┌──────────────┼───────────────────────┼─────────────────────────┐
│              │ Session Layer         │                          │
│    ┌─────────────────────────────────┴─────┐                  │
│    │  ISessionConnection                    │                  │
│    │  ISessionManager                       │                  │
│    │  IReliabilityHandler                   │                  │
│    │  Session State & Statistics            │                  │
│    └──────────────────────────────────────┘                  │
└──────────────┬───────────────────────┬─────────────────────────┘
               │                       │
┌──────────────┼───────────────────────┼─────────────────────────┐
│              │ Transport Layer       │                          │
│  ┌───────────────────────┐ ┌──────────────────────────┐         │
│  │  ITransportConnection │ │  ITransportListener      │         │
│  ├───────────────────────┤ ├──────────────────────────┤         │
│  │ TCP Implementation    │ │ TCP Implementation       │         │
│  │ UDP Implementation    │ │ UDP Implementation       │         │
│  └───────────────────────┘ └──────────────────────────┘         │
│                                                                   │
│    Frame Protocol: [4-byte Length][Data]                        │
└───────────────────────────────────────────────────────────────┘
         ↓                           ↓
    ┌─────────┐              ┌─────────────┐
    │   TCP   │              │    UDP      │
    │ Socket  │              │  Datagram   │
    └─────────┘              └─────────────┘
```

## Request Flow Diagram

```
Client Connection
    ↓
┌─────────────────────────────┐
│  Transport Layer            │
│  Accept connection          │
│  Create ITransportConnection│
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Session Layer              │
│  Create ISessionConnection  │
│  Initialize metadata        │
│  Track statistics           │
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Packet Reception           │
│  Receive raw bytes          │
│  Extract packet ID          │
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Validation Layer           │
│  Look up validator          │
│  Validate packet data       │
│  (Skip if HotPath)          │
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Routing Layer              │
│  Determine route            │
│  Check if relay/direct      │
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Packet Dispatch            │
│  Look up handler            │
│  Invoke HandleAsync()       │
└──────────┬──────────────────┘
           ↓
┌─────────────────────────────┐
│  Application Handler        │
│  Process packet             │
│  Generate response          │
└─────────────────────────────┘
```

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────┐
│                  Your Application                        │
└──────────────┬──────────────────┬──────────────────────┘
               │                  │
        ┌──────────────┐    ┌─────────────────┐
        │   Register   │    │  Create Session │
        │   Handlers   │    │   Listeners     │
        └──────┬───────┘    └────────┬────────┘
               │                     │
               ↓                     ↓
        ┌──────────────────────────────────┐
        │  NetworkApplicationStack          │
        │  ├─ SessionManager               │
        │  ├─ PacketRegistry               │
        │  ├─ HandlerRegistry              │
        │  ├─ ValidationPipeline           │
        │  ├─ RoutingStrategy              │
        │  ├─ Dispatcher                   │
        │  └─ Diagnostics                  │
        └──────┬──────────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
    ↓                     ↓
┌──────────────┐  ┌──────────────┐
│ TCP Listener │  │ UDP Listener │
└──────┬───────┘  └───────┬──────┘
       │                  │
    Accept Connection   Accept Datagram
       │                  │
       ↓                  ↓
┌────────────────────────────────┐
│   Session Management           │
│   - Statistics                 │
│   - State Tracking             │
│   - Metadata                   │
└────────────────────────────────┘
```

## Packet Flow Diagram

```
Raw Bytes Received
    ↓
┌─────────────────────┐
│ Parse Packet ID     │
│ (First 16 bytes)    │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ Lookup Descriptor   │
│ By Packet ID        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ Deserialize         │
│ Using Serializer    │
│ from Descriptor     │
└──────────┬──────────┘
           ↓
    ┌─────────────────────┐
    │ Validation Required?│
    │ (Metadata Check)    │
    └──┬─────────────┬────┘
       │             │
      YES            NO
       │             │
       ↓             │
┌──────────────┐    │
│ Validate     │    │
└──────┬───────┘    │
       │            │
    Valid?          │
    ↓               │
    └────┬──────────┘
         ↓
┌─────────────────────┐
│ Lookup Handler      │
│ By Packet Type      │
└──────────┬──────────┘
           ↓
    ┌──────────────────┐
    │ Handler Found?   │
    └──┬────────┬──────┘
       │        │
      YES       NO
       │        │
       ↓        ↓
    Handle   Log Warning
    Packet
       ↓
    Complete
```

## Data Structure Diagram

```
Session
├─ SessionId (Guid)
├─ State (SessionState enum)
├─ Metadata
│  ├─ ConnectedAt (DateTime)
│  ├─ Username (string?)
│  └─ Items (Dictionary<string, object>)
├─ Statistics
│  ├─ BytesSent (long)
│  ├─ BytesReceived (long)
│  ├─ PacketsSent (long)
│  └─ PacketsReceived (long)
└─ Transport (ITransportConnection)

Packet Descriptor
├─ PacketId (Guid)
├─ PacketType (Type)
├─ Metadata
│  ├─ Name (string)
│  ├─ Category (PacketCategory enum)
│  ├─ Reliability (ReliabilityMode enum)
│  ├─ RequiresValidation (bool)
│  └─ RequiresRouting (bool)
└─ Serializer (IPacketSerializer)

Route Descriptor
├─ Kind (RouteKind enum)
├─ DestinationSession (Guid?)
├─ StreamId (string?)
└─ ClusterService (string?)
```

## State Diagram - Session Lifecycle

```
┌──────────┐
│ Created  │
└────┬─────┘
     │
     ↓
┌──────────────┐
│ Handshaking  │
└────┬─────────┘
     │
     ├─ Identity Request
     │
     ↓
┌──────────────┐
│ Authenticate │
└────┬─────────┘
     │
     ↓
┌──────────┐
│ Authed   │
└────┬─────┘
     │
     ↓
┌──────────┐
│  Active  │◄─── Receive/Send packets
└────┬─────┘
     │
     ├─ Client Close
     │ Server Close
     │ Timeout
     │
     ↓
┌──────────┐
│ Closing  │
└────┬─────┘
     │
     ↓
┌──────────┐
│ Closed   │
└──────────┘
```

## Validation Flow Diagram

```
Packet Received
    ↓
┌─────────────────────┐
│ Check Reliability   │
│ Mode               │
└─────┬────┬──────┬──┘
      │    │      │
   RELIABLE│   UNRELIABLE
      │    │      │
    HOT   │      └─→ Skip Validation
   PATH  │
      │  ↓
      │ Check Metadata
      │ .RequiresValidation
      │  │
      │ YES
      │  │
      ↓  ↓
┌──────────────────┐
│ Look up Validator│
│ In Registry      │
└────────┬─────────┘
         │
    ┌────┴─────┐
    │           │
 Found      Not Found
    │           │
    ↓           ↓
 Validate    Success
    │
    ↓
 Result
 ├─ Valid → Continue
 └─ Invalid → Error
```

## Thread Safety Model

```
Concurrent Access
    ↓
┌─────────────────────┐
│ Registry Layer      │ ← ConcurrentDictionary
│ (Thread-Safe)       │   (Lock-Free lookups)
└──────────┬──────────┘
           │
┌──────────┴──────────┐
│ Session Statistics  │ ← Object Lock
│ (Thread-Safe)       │   (Rare contention)
└──────────┬──────────┘
           │
┌──────────┴──────────┐
│ Individual Session  │ ← No locking needed
│ (Single-threaded)   │   (One request at a time)
└─────────────────────┘
```

## Factory Pattern Usage

```
Your Code
    │
    ↓
┌────────────────────────────┐
│ NetworkApplicationFactory  │
│ ├─ .WithSessionManager()   │
│ ├─ .WithPacketRegistry()   │
│ ├─ .WithRoutingStrategy()  │
│ └─ .CreateDefault()        │
└────────────┬───────────────┘
             │
             ↓
┌────────────────────────────┐
│ NetworkApplicationStack    │
│ ├─ SessionManager          │
│ ├─ PacketRegistry          │
│ ├─ HandlerRegistry         │
│ ├─ ValidationPipeline      │
│ ├─ RoutingStrategy         │
│ ├─ Dispatcher              │
│ ├─ RelayService            │
│ ├─ StreamRouter            │
│ └─ Diagnostics             │
└────────────────────────────┘
```

## Dependency Injection Model

```
Application
    │
    ├─ Depends on NetworkApplicationFactory
    │
    └─ Factory creates
       ├─ SessionManager
       ├─ PacketRegistry
       ├─ ValidationRegistry
       ├─ Dispatcher
       │  ├─ Depends on PacketRegistry
       │  └─ Depends on HandlerRegistry
       │
       └─ All components are swappable
          (Via custom implementations)
```

---

**Visual Guide Complete** ✓

Use these diagrams to understand:
- System architecture and layers
- Request/packet processing flow
- Component interactions
- Data structures
- State transitions
- Thread safety model
- Factory and DI patterns

Refer back to these when building custom implementations!
